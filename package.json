{
  "name": "proyecto_curso",
  "version": "1.0",
  "description": "Proyecto del curso",
  "scripts": {
    "build": "tsc index.ts",
    "build:tsc": "tsc index.ts",
    "start": "tsc index.ts",
    "build:app": "tsc app.ts",
    "transpile": "tsc index.ts && tsc app.ts",
    "transpile2": "npm run start && npm run build:app",
    "transpile3": "tsc index.ts && npm run build:app",
    "transpile4": "tsc index.ts app.ts",
    "transpile5": "tsc",
    "test": "tsc index.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^18.15.9",
    "nodemon": "^2.0.21",
    "typescript": "^4.9.5"
  }
}
